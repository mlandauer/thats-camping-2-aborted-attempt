That's Camping 2.0!

You need to serve the page through a webserver locally otherwise the geolocation stuff won't work. To do that there's
a super-simple to use static web server, written in Ruby.

To install:
gem install heel

To run:
heel
